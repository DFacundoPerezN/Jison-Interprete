{
  "name": "interprete",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:parser": "jison parser/simplicode.jison -o parser/parser.js",
    "start": "node index.js",
    "dev": "nodemon --watch parser/simplicode.jison --watch src --ext js,json,jison --exec \"npm run build:parser && node index.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "description": "",
  "devDependencies": {
    "jison": "^0.4.18",
    "nodemon": "^3.1.10"
  }
}
